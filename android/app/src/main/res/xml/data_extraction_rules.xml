<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ backup/transfer rules.

  The WebView's localStorage holds the Supabase session, wallet connection
  state, and the offline audio cache. None of that should leave the device in a
  cloud backup or a device-to-device transfer: a restored session token on a
  different device is an account takeover vector, and the audio cache is large
  and re-downloadable.

  allowBackup="false" in the manifest already blocks this on every API level;
  these rules make the intent explicit for API 31+.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
