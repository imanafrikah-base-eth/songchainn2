# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
# package-lock.json is tracked on purpose since 7 Sep 2026: Vercel installs from it,
# so production builds with the exact dependency versions verified locally.
# (Every build from 22 Aug to 6 Sep failed because a fresh install drifted.)
bun.lockb

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

Song Arts/
*.tsbuildinfo

# Android / Capacitor
# The android/ project itself IS committed (it holds the manifest, icons and
# gradle config). Only signing secrets, local paths and build output are not.
android/keystore.properties
android/*.keystore
android/*.jks
android/local.properties
android/.gradle/
android/build/
android/app/build/
android/app/release/
android/capacitor-cordova-android-plugins/
# Copied from dist/ on every `cap sync`, so it is build output not source.
android/app/src/main/assets/public/
android/app/src/main/assets/capacitor.config.json
*.apk
*.aab

# Private credentials — never commit this folder
.private/

# Onchain minting tooling — may contain wallet keys, logs, and tx data
scripts/zora-mint/

# Local audit/debug artifacts
e2e-audit/
shots/
screenshot.png
state.json
skills-lock.json
_verify_tmp.cjs
test-results/
playwright-report/
.agents/
.claude/
.mcp.json
this is me SONGCHAINN.txt
supabase/.temp/
.ui-inspect-session.json
artist-credentials.json
ARTIST_LOGINS.md
.artist-session*
.empty-session.json

# Private handover sheet, never commit
ARTIST-HANDOVER.md

# Not part of this app, and this repo is PUBLIC.
# BattleZone/ holds separate products with their own git repos. A blanket
# `git add -A` used to stage all of them into songchainn2, including a whole
# other Next.js app with its own admin console and auth routes.
BattleZone/AUDIO ENGEN/
BattleZone/africa-battle-live-ui/

# Material sent in by users for debugging. Never publish someone's video.
vids from users for fixing/

# Working files from the Diyama review, not source.
e2e/.auth/

# Launcher key must never be committed
.launcher-key
scripts/coins/*.key.json

# Database dumps taken for rollback while fixing BattleZone. Real user rows; never public.
scripts/*backup*.json
